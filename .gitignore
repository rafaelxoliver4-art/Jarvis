# Secrets — never commit
.env

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# Assistant output and local memory
generated/*
!generated/.gitkeep
memory/*.local
memory/*.db

# Phase 1.5 — tool-usage telemetry (track folder, not contents)
logs/*.jsonl

# Voice-test orchestration artifacts (regenerated by every test run)
# NOTE: voicetest_*.log capture live CONVERSATION TRANSCRIPTS — never commit (privacy).
test_stdout.txt
test_stderr.txt
logs/voicetest_*.log
logs/last_voicetest.txt
logs/voicetest_pid.txt
.commitmsg.tmp

# OS / editor
.DS_Store
.vscode/
.idea/
